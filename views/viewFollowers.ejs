<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <% for(let follower of user.followers){ %> <%= follower.firstname %> <%= follower.lastname %>
    @<%= follower.username %>
    <form action="/user/profile/<%= follower.id %> " method="post">
      <button type="submit">
        <% if(user.following.includes(follower.id)){ %> Following <% } else{ %> Follow <% } %>
      </button>
    </form>
    <% } %>
  </body>
</html>
